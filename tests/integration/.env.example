# Quikturn Logos API — Integration Test Keys
# Copy this file to .env and replace with real test keys.
# See README.md for key provisioning instructions.
#
# Publishable key (qt_ or pk_ prefix) — used for browser client tests
QUIKTURN_PUBLISHABLE_KEY=qt_your_publishable_key_here
#
# Secret key (sk_ prefix) — used for server client tests
QUIKTURN_SECRET_KEY=sk_your_secret_key_here
